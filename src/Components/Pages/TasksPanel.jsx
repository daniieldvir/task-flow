export default function TasksPanel() {
  return (
    <div>
      <h2>Tasks</h2>
      {/* <ul>
        {tasks.map((task) => (
          <li key={task.id}>
            <strong>{task.name}</strong> – {task.status} – {task.author}
          </li>
        ))}
      </ul> */}
    </div>
  );
}
